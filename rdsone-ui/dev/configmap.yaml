kind: ConfigMap 
apiVersion: v1 
metadata:
  name: rdsone-ui-configmap
  namespace: ingress
data:
  # Configuration values can be set as key-value properties
  #DATABASE: dbname
  #DATABASE_URL: db_url
